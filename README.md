# My custom set commands:

- `rebh` - alias for rebuilding **home-manager** and committing to this repo
- `reb` - alias for rebuilding **NixOs** and committing to this repo
- `confpush` - alias for pushing commits to remote
